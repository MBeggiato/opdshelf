version: '3.8'
services:
  opds-server:
    build: .
    image: opdshelf:dev
    container_name: opds-server-dev
    ports:
      - "3001:3000"
    volumes:
      - ./books:/app/books
    restart: unless-stopped
    environment:
      - PORT=3000
      - HOST=0.0.0.0

